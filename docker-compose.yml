services:
  naukri-updater:
    image: ${IMAGE_NAME:-ghcr.io/honey0632/naukri-auto-updater-java:latest}
    container_name: naukri-auto-updater
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data